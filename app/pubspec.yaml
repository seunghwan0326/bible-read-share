name: bible_read_share
description: 함께읽는성경 - Android/iOS 공용 Flutter 앱
publish_to: "none"
version: 1.2.1+3

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ">=1.2.0 <2.0.0"
  shared_preferences: ">=2.3.0 <3.0.0"
  share_plus: ">=10.0.0 <13.0.0"
  uuid: ">=4.5.0 <5.0.0"

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ">=4.0.0 <7.0.0"

flutter:
  uses-material-design: true
